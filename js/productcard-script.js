/* рабочий код для окна .templates-popup на карточках */




/* var productcardlinks = document.querySelectorAll(".templates-img-link");
productcardlinks.forEach(function (productcardlink) {
  productcardlink.addEventListener("mouseenter", function (evt) {
    evt.preventDefault();
    var productcardpopup = this.nextElementSibling;
    productcardpopup.classList.add("productcard-modal-show");
  });

  productcardlink.addEventListener("mouseleave", function (evt) {
    evt.preventDefault();
    var productcardpopup = this.nextElementSibling;
    productcardpopup.classList.remove("productcard-modal-show");
  });
});

var productcardpopups = document.querySelectorAll(".templates-popup");
productcardpopups.forEach(function (productcardpopup) {
  productcardpopup.addEventListener("mouseenter", function (evt) {
    evt.preventDefault();
    this.classList.add("productcard-modal-show");
  });

  productcardpopup.addEventListener("mouseleave", function (evt) {
    evt.preventDefault();
    this.classList.remove("productcard-modal-show");
  });
}); */
